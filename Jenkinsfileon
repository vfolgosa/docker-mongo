node {
    checkout scm

    docker.withRegistry('http://54.233.110.154:5043', 'docker-repository-credentials') {

        def customImage = docker.build("mongo:${env.BUILD_ID}")

        /* Push the container to the custom Registry */
        customImage.push()
		customImage.run()
    }
}